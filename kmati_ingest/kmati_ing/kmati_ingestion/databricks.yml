# This is a Databricks asset bundle definition for kmati_ingestion_framework.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: kmati_ingestion
  uuid: b9180957-23af-4aa8-91ba-22116535da4f

include:
  - resources/*.yml
  - resources/*/*.yml
  - resources/*.yml
  - configs/*.yaml

artifacts: {}

# artifacts:
#   default:
#     type: whl
#     build: poetry build
#     path: . 

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-4ee2d87f-810b.cloud.databricks.com
      #https://dbc-9972b262-efda.cloud.databricks.com
    variables:
      catalog: workspace
      schema: default
  prod:
    mode: production
    workspace:
      host: https://dbc-4ee2d87f-810b.cloud.databricks.com
      #https://dbc-9972b262-efda.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/swaroopa12003@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/swaroopapatibandla@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: default
    permissions:
      - user_name: swaroopapatibandla@gmail.com
        level: CAN_MANAGE
